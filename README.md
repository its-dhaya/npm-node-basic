# create-node-basic 🚀

A simple Node.js project generator that quickly creates a basic starter structure.

This package helps you initialize a clean Node project with essential files like:

- `src/`
- `index.js`
- `.gitignore`
- `.env.example`

---

## 📦 Installation (No install needed)

Use directly with npm:

```bash
npm init node-basic myProject
